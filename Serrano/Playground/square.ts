let area = (side : number) : number => {return(side*side)};
let peri = (side : number) : number => {return(4*side)};

console.log("Input : 4");
console.log("Output : Perimeter = "+ peri(4) + ", " + "Area = "+area(4));